1. 일단 해당 브랜치에서는 frontend 폴더에서 리액트 문법을 기반으로 로그인 기능들을 먼저 구현해보려는 것이 목적이야.
   일단 notion의 mcp to design 페이지내의 thanksquirrel 페이지에 접근해서
   로그인 관련 UI 및 기능들을 구현해주었으면 해
   일단 기능 부분은 일반 로그인 먼저 구현해주고, 추후에 간편 로그인(카카오, 네이버, 구글 로그인)도 구현해볼거야
   UI에서 일단 다람쥐 캐릭터, "달램이"가 처음에는 로그인 페이지에서 반겨주는 애니메이션을 넣었으면해
   "달램이" 이미지는 supabase의 storage에 들어있고, thanksquirrel-image에 모든 사진들이 들어있어
   내가 로그인 창에서 입력을 진행하게 되면 "달램이"가 눈을 가리는 사진으로 변경하였으면 해
   달램이 이미지는 notion에서도 확장자명을 지정해놓았는데,
   일단 로그인 창에서 숨어있을 때에는 기본 이미지(character-web.webp)형태로 로그인 창에서 숨어있다가
   다시 반겨주는 이미지(character-hello.webp)로 변경되면서 로그인 창에서 나타나고
   로그인 창에서 입력을 진행하게 되면 눈을 가리는 이미지(character-hide.webp)로 변경되도록 하면 좋겠어
   1-1. 전체적으로 괜찮기는 한데 일단 UI랑 일부 애니메이션 부분 좀만 바꿔줘, 내가 mcp to design에서
   만들어놓은 로그인 UI를 기반으로 만들어보았으면 해 배경화면은 background.webp를 통해 전체적인 배경을 설정해주고
   달램이가 눈을 감는 시점은 입력 창에 이메일이나 비밀번호가 입력이 된 상태에서 계속 진행되어야 돼
   (만약에 입력창에 이메일이나 비밀번호가 입력이 안된 상태라면 달램이는 눈을 뜨고 반겨주는 애니메이션을 유지해야해)
   비밀번호 찾기나 회원가입 버튼도 일단은 만들어만 놔 나중에 내가 거기서 기능을 추가해볼 것이니까
   1-2. 그리고 달램이가 말하는 말풍선 같은 것도 추가해줘 참고로 notion의 mcp to design의 thanksquirrel 페이지에서 나오는 말풍선 레이아웃들을 참고로해주면 돼 등장할 때 말풍선도 opacity값을 0에서 1로 주면서 transition도 0.3초 정도로 스르륵 나타나는 식으로 표현하면 좋겠어, 그리고 인사할 때 대사는 "다람다람! 누구신지 알려주시면 감사하겠습니다람!”이라고 표현해주고,
   아이디/비밀번호 입력할 때의 대사는 "다람다람! 안보고있을거니 걱정안하셔도 됩니다람!”으로 표현하면 좋겠어
   참고로 말풍선은 자주 쓸 것이니까 컴포넌트 형태로 대사같은 것들 바꿔서도 자주 사용할 수 있도록 만들면 좋겠어
   그리고 달램이가 눈을 감는 조건에 이메일/비밀번호 입력창에 값이 입력이 되어있을 때에 더해 각 입력창에 포커스 되어있을 때도 포함해줘
2. 이번에는 회원가입 관련 ui를 만들어보려고 하거든 로그인 창에서 회원가입 버튼을 누르면 회원가입 관련 팝업이 나타나게 해줘 참고로 notion의 mcp to design 페이지의 thanksquirrel에 보면 회원가입 디자인이 있어 그것을 참고해주면 좋아 일단 회원가입창에 필수로 넣어볼만한 것은 이메일(필수 입력값), 비밀번호(필수 입력값, 8자리 이상, 특수문자, 숫자, 영어 포함), 비밀번호 확인(필수 입력값), 별명(이메일 확인 후 해당 이메일이 기본값, 따로 바꾸기 가능), 성별(기본은 비밀, 남자나 여자로 바꿀 수 있음), MBTI(기본값은 infp(supabase 데이터베이스에 기본값으로 존재), 추후에 select로 바꿀 수 있음), 취미(기본값 빈 문자열), 특기(기본값 빈 문자열)가 있어 이 데이터들은 supabase의 데이터베이스에 있어 회원가입에 성공하면 거기서 비밀번호를 제외하고 형식에 맞는 다른 정보들이 다 저장되어야 해 로그인/회원가입 방식은 supabase이 인증 서비스를 통해 할거야 회원가입에 성공해서 데이터가 저장되면 로그인하게 되면 회원가입을 하였을 때의 정보를 기반으로 이메일과 비밀번호를 입력하면 로그인이 되도록 해줘
   2-1. 회원가입을 통해 Authentication에서 회원가입했을 때 데이터가 정상적으로는 올라와 그런데 데이터베이스에서는 스키마에 유저 관련 데이터들이 올라오지 않았는데 그 부분에 관해서 회원가입 했을 때 정상적으로 데이터들을 올려놓을 수 있도록 수정해줘, 그리고 회원가입 한 후에 로그인을 하려는데 제대로 된 이메일과 비밀번호를 입력했는데도 로그인이 안되는 현상이 발생하고 있어 그 부분에 관해서도 수정해줘, 그리고 달램이가 로그인 창에서 회원가입 창이 뜨면 눈을 가리도록 해줘, 그리고 회원가입이나 로그인이 완료되면 중앙에 말풍선같은 확인 창이 뜨고, 달램이가 로그인창 말고 밑에서부터 애니메이션으로 올라와서 상체 부위가 보이기까지 만들어줘 참고로 디자인은 notion의 mcp to design 페이지의 thanksquirrel에 보면 관련 디자인들이 있어
   2-2. 로그인은 정상적으로 동작하는데 회원가입할 때 이메일을 자주 보내다보니까 벌써 이메일 전송량에 한계가 와서 회원가입을 못하는 상황이야 일단 supabase에서 회원가입을 진행할 때 이메일 인증을 하거나 이메일을 보내는 기능을 제거하고, 회원가입을 진행할 수 있도록 수정해줘 이메일 인증은 나중에 비밀번호 찾기 기능 구현해볼때 해보려고 해 그리고 로그인 성공해서 나오는 달램이의 이미지는 character-hello.webp로 변경해줘, 그리고 로그인 후에 나타나는 가운데에 오는 말풍선의 내용은 로그인을 했을 경우에는 "다람다람! 우리 동네 사람이었다람! 편히 쉬시면 좋겠습니다람!", 그리고 회원가입을 했을 경우에는 "다람다람! 처음이라 쉽지 않을텐데 용기내서 우리 동네 와주셔서 정말 감사합니다람! 수고 많으셨을텐데 우선 우리 동네에서 푹 쉬고 있어주시면 좋겠습니다람!”이라는 대사를 했으면 좋겠어 그리고 회원가입을 했을 경우에도 인증이 되어서 로그인 성공때와 같이 달램이가 밑에서부터 애니메이션으로 올라와서 상체 부위가 보이기까지 만들어줘, 그리고 로그인 또는 회원가입에 성공하면 로그인 창은 밑으로 0.5초 정도 slideout으로 스르륵 내려가는 연출을 하였으면 좋겠어, 로그인 성공후 나오는 말풍선은 좀 더 크게 만들어줘, 가로는 300px, 세로는 175px 정도로, 그리고 로그인 성공후의 말풍선 하단에 "알겠다람!"이라는 문구가 적힌 버튼도 추가해줘, 일단 버튼에 대한 동작은 나중에 진행할 거야 UI상에 보여지기만 하면 돼
   2-3. 이번에는 회원가입을 진행할 때 로그인 창이랑 달램이가 내려가기만 하고 로그인할 때 처럼 인증이 되어서 달램이가 사용자를 반겨주는 애니메이션이 없이 다시 로그인 창이랑 달램이가 같이 올라오는 현상만 있어 이것을 좀 고쳐주었으면 해
   2-4. 다른 것들은 잘 고쳐졌는데, 이번엔 로그인/회원가입 후에 나타나는 말풍선 하단에 버튼이 없어 그 버튼만 바로 추가해주었으면 해, 버튼에는 "알겠다람!"이라는 문구 유지하고, 버튼을 클릭했을 때의 기능은 아직 구현하지 않아도 돼 그리고 말풍선 크기가 아직도 너무 작은데 가로 300px 세로 175px 정도로 만들어줘
3. 이번에는 구글 로그인을 구현해보려고 하거든, 참고로 구글 OAUTH에서 등록을 다해놓았고, SUPABASE에서도 구글 OAUTH를 연동해놓은 상황이야 이제 이것들을 기반으로 구글 로그인을 진행하게 될 때 똑같이 회원가입이나 로그인이 적용되게 해주고, 회원가입을 하게 될 때 유저 관련 데이터베이스에서도 관련 데이터가 등록되게 해줘, 참고로 구글 로그인으로 가져오는 데이터는 email, 그리고 email에서 @gmail.com을 뺀 앞의 내용을 가져오면 돼 나머지 값들은 데이터베이스에 추가가 될 것이야 즉, 일반 회원가입을 하는 것처럼 구글 회원가입도 동일하게 진행해주면 되고, 구글 로그인을 진행할 때에도 구글 회원가입을 통해 가입된 유저일 경우에 그 로그인을 허용해주어야 해
   3-1. 이번에는 네이버 로그인을 구현해보려고 하거든, 아래 내용을 참고해줘

## 환경 및 목적

Stack: React (또는 Next.js), Supabase Auth 사용
목표: custom:naver 프로바이더를 통해 로그인을 구현하고, 로그인 성공 시 profiles 테이블에 사용자 정보를 자동으로 저장/업데이트하는 로직 구현.

## 인증 설정 (OAuth)

Provider ID: custom:naver를 사용합니다.
필요 Scopes: email, nickname, profile_image, gender
보안: .env 파일은 직접 수정하거나 읽지 말고, 코드 내에서 process.env.REACT_PUBLIC_SUPABASE_URL 등 환경 변수 이름만 사용하여 구조를 잡아주세요. (키값은 내가 직접 넣을 예정)

## 데이터 매핑 로직 (핵심)

네이버 로그인이 성공하면 session.user.user_metadata 내의 데이터를 분석하여 아래 규칙에 따라 profiles 테이블에 데이터를 upsert 하거나 insert 하세요.
별명 (Nickname): - 네이버에서 제공한 nickname이 있다면 최우선으로 사용.
nickname이 없다면 이메일 주소의 @ 앞부분을 추출하여 사용.
성별 (Gender): - 네이버에서 정보를 가져왔다면 해당 값을 사용.
정보가 없다면 기본값을 '비밀'로 설정.
프로필 사진 (Profile Image): - 네이버에서 준 이미지 URL을 가져오되, 나중에 Storage에 올리거나 별도 DB에서 처리할 수 있도록 변수화하여 profiles 테이블의 avatar_url 컬럼에 저장.
신규/기존 유저 판별:
profiles 테이블에 해당 user_id가 없다면 신규 가입으로 간주하고 초기 데이터를 생성.
이미 존재한다면 로그인으로 취급 (필요 시 최신 정보로 업데이트).

## 구현 요청 사항

onAuthStateChange를 사용하여 로그인 이벤트를 감지하는 로직을 작성해줘.
네이버는 응답 데이터가 response 객체 안에 한 번 더 감싸져 있을 수 있으니, 데이터 경로를 안전하게 파싱하는 로직(Optional Chaining 등)을 포함해줘.
profiles 테이블에 들어갈 nickname, gender, avatar_url, last_check_in_at 등의 필드를 포함한 insert 쿼리를 작성해줘.

3-2. 지금 네이버 로그인 기능 구현과 관련하여 문제점이 좀 보이는 상황이야, 네이버 로그인을 진행을 하였을 때, supabase의 authentication에 유저 관련 데이터는 정상적으로 올라오는데, 유저 스키마에서는 로그인/회원가입을 한 번 진행해도 유저 데이터가 여러번 올라오는 현상이 발생하고 있어, 게다가 이 유저 데이터는 회원가입을 할 때 한번만 적용이 되어야 하는데, 로그인 할 때도 적용이 되어버리는 현상이 발생하고 있어 그리고 지금 컴퓨터에서는 안그러는데 어제 다른 컴퓨터에서는 네이버 로그인을 마친 상태에서 다시 웹사이트를 새로고침했을 때, 강제적으로 회원가입과 같은 애니메이션이 강제적으로 나타나는 현상이 발생하고 있었거든, 게다가 이미 authentication에 이미 유저 데이터가 있는 상태에서 로그인을 했는데도 달램이가 로그인을 진행할 때의 대사가 아닌, 회원가입을 할 때의 대사가 나오는 현상이 발생하고 있어 이 문제들을 해결해주었으면 해

3-3. 네이버 로그인을 진행하게 될 때, authentication이랑, 유저 스키마에서는 정상적으로 데이터가 한번씩 들어오고, 로그인을 하게 되면 데이터가 더 추가되는 현상은 해결되었는데, 문제는 네이버 로그인을 진행하게 될 때, 회원가입을 할 때에도 로그인을 했을 때의 대사가 나오게 되거든, 이 부분 해결해주었으면 좋겠어 그리고 구글 로그인을 통해 회원가입을 하게 될 때에는 유저 스키마에서 데이터가 여러개가 저장되는 현상도 발생하고 있어 그리고 일반 로그인을 하게 되면 유저 스키마 내의 유저 데이터나 authentication에 유저 데이터가 없는 상태에서 회원가입을 한번 하게 되면 내가 유저 스키마나 authentication의 유저 데이터를 제거해도 계속 웹페이지 새로고침 할 때 마다 회원가입을 했을 때의 애니메이션이 나타나는 현상이 발생하고 있어 이 문제들을 해결해주었으면 해 그리고 일반 로그인, 간편 로그인 진행했을 때 공통적으로 유저 스키마의 email 속성에 email 데이터가 제대로 저장이 되지 않는 문제가 있어 이 부분도 수정되었으면 해



3-4. 이번에는 카카오 로그인을 진행해보려고 해, 일단 카카오 로그인을 진행하기 위해 supabase에서 카카오 로그인을 위해 필요한 작업들은 전부 마친 상황이야 로그인 창에서 간편 로그인을 진행하게 된다면, 카카오 로그인을 진행할 때에도 다른 로그인들과 마찬가지로, authentication에 유저 데이터가 한번만 저장이 되고, 데이터베이스에서도 유저 데이터가 다른 로그인들처럼 동일하게 유저 데이터가 한번만 저장이 되도록 수정해주었으면 해, 그리고 카카오 로그인에서 동의받기로 한 것들은account_email(이메일), profile_image(프로필 사진), profile_nickname(닉네임) 이렇게 3개가 있어 모두 유저 스키마에 저장을 해볼거야 account_email은 필수적으로 받는 것이고, email 칼럼에 저장해주었으면 해, profile_image는 선택적으로 받는 것이고, avatar_url 칼럼에 저장해주었으면 해, profile_nickname은 선택적으로 받는 것이고, 받지 못했다면 email에서 @이메일명.com을 뺀 앞의 내용을, 받았다면 그 내용을 nickname 칼럼에 저장해주었으면 해


3-5. 카카오 로그인 자체는 정상적으로 작동을 하고, 유저 스키마랑 authentication에 유저 데이터가 한번씩만 저장이 되는 현상도 해결되었는데, 문제는 카카오 로그인을 진행했을 때, 로그인/회원가입 했을 때 달램이가 반기는 애니메이션이 없고, 그냥 로그인창만 무한대로 반복해서 뜨기만 해 이런 현상 좀 고쳐주었으면 해


4. 지금 간편 로그인을 진행할 때 각 로그인들의 보안이 좀 허술해 보이거든? 예를 들어 구글같은 경우에는 한번 계정을 선택하면 그 이후에 인증 절차도 없이 바로 로그인이 성공하고, 네이버/카카오 같은 경우에는 처음에는 인증을 진행하는데 그 이후로 로그아웃을 진행하고 다시 로그인을 하게 되면 인증 절차도 없이 바로 로그인이 성공해버려, 이 부분들을 좀 더 보안을 강화하는 방향으로 수정해주었으면 해 예를 들어 구글 로그인은 계정 선택 후 반드시 인증을 진행하도록 하고, 카카오/네이버 로그인은 로그아웃을 한 이후에도 항상 다시 인증을 진행할 수 있도록 수정해주었으면 해 


4-1. 네이버/카카오는 정상적으로 로그인시마다 인증이 되는데, 구글은 로그인 할 때마다 제대로 인증절차를 거치지 않는 문제가 있어, 내가 만약에 구글 로그인을 진행을 하잖아? 계정을 선택한 이후에 인증을 선택해야 돼 인증을 하게 될 때에는 구글 비밀번호를 치든, qr코드를 찍든 상관없어, 어짜피 구글에서 처리하는대로 하고, 우리는 구글 로그인을 진행시 본인인증을 넣어보는 것이 목적이야 그러니까 구글 로그인시의 인증 기능도 같이 추가해 


4-2. 내가 supabase에서 confirm email이랑 url configuration을 진행을 완료한 상황이야 이제 내가 원하는 것은 회원가입을 진행할 때, 회원가입 창에 이메일 인증 버튼이 뜨게 되고, 이메일 인증을 하게 되면 supabase의 자체 인증 시스템을 통해 이메일 인증을 진행하게 되면서 자동으로 로그인이 진행이 돼, 참고로 이메일 인증이 되지 않았다면 절대로 회원가입이 되지 않게 할 거야


5. 이제 비밀번호 찾기 기능을 구현해볼거야, 우선 로그인 창에서 비밀번호 찾기 버튼을 누르면, 비밀번호 찾기 창이 뜨게 되고, 비밀번호 찾기 창에서 이메일을 입력하면 supabase의 자체 인증 시스템을 통해 이메일 인증을 진행하게 되면서 새 비밀번호로 바꾸라는 창이 뜨게 될 거야, 이메일 인증이 완료되면 새 비밀번호를 입력할 수 있게 되고, 새 비밀번호를 입력하고 확인을 누르면 비밀번호가 변경되면서 자동으로 로그인이 진행이 돼, 참고로 이메일 인증이 되지 않았다면 절대로 비밀번호 찾기가 되지 않게 할 거야, 디자인은 notion의 mcp to design/thanksquirrel 페이지에 관련 이미지가 있어, 비밀번호 찾기 UI 1,2에 있는 사진 부분을 참고하면 되고, 일단 전체 비밀번호 찾기 UI는 현재 웹페이지의 로그인 창의 레이아웃 환경을 바탕으로 만들어주었으면 해


6. 지금 @src/components애서 전반적으로 로그인 및 회원가입이나 비밀번호 찾기 기능들을 전반적으로 구현을 했잖아 혹시 이 중에서 보안적으로 위험한 요소들이 있을까(SQL 인잭션이나 XSS 공격 등 보안 관련 위험 가능성이 있는 코드) 그런 부분들이 있다면 기획서 한국어로 정리해주고, 어떻게 수정할 것인지까지 알려줘서 수정 작업 진행해주었으면 해


7. 로그인이 끝난 뒤에 확인하기 버튼을 누르면 기존의 환영하던 달램이(character-hello.webp)와 말풍선 팝업 창이 아래로 스르륵 사라지면서 애니메이션이 나타나면서 헤더는 화면 위쪽의 바깥에서 아래로 스르륵 등장하고, 푸터는 화면 아래쪽의 바깥에서 위로 스르륵 등장하는 애니메이션을 추가하면서 일반 상태의 달램이 캐릭터(character-web.webp)도 화면 아래에서 등장하는 애니메이션을 추가해주었으면 해, 전체적인 등장 애니메이션들은 0.3초 간격으로 움직였으면 하고, 환영하는 달램이가 화면상에 사라진 후에 일반 상태의 달램이 캐릭터가 등장하는 거야 디자인은 notion의 mcp to design/thanksquirrel 페이지에 관련 이미지가 있어, 로그인 후 애니메이션 1,2, 그리고 로그인 후 애니메이션 3(기본 화면)에 있는 사진 순으로 참고하면 돼, copyright 2026 jshh001206@gmail.com All rights reserved. 문구를 푸터의 맨 아래의 중앙에 추가해주고, 헤더에는 오른쪽에 편지 아이콘, 그리고 프로필 아이콘을 추가할거야 프로필 아이콘은 유저에게서 받아온 기본적으로 받아온 프로필 이미지를 쓰거나 아니면 character-icon.png 이미지(supabase의 thanksquirrel-webpage-image 스토리지)를 사용해주었으면 해, 헤더 왼쪽에는 기본적으로 로고를 넣어주고, 로고는 thanksquirrel-webpage-image 스토리지에 logo.webp 이미지를 사용해주었으면 해, GEMINI.md 파일을 참고해보아도 좋아

7-1. 로그인을 처음 진행하였을 때에는 제대로 푸터, 헤더 부분들이 나타나면서 달램이도 같이 등장하는 것은 잘 되었는데, 로그인이 될 때 마다 해당 애니메이션이 꼭 발생해야 되거든, 그런데 지금 내가 지금 로그인 후 확인 버튼을 눌렀을 때에는 처음에는 동작이 되는 것을 볼 수 있었는데 그 이후로는 확인 버튼을 계속 눌러도 애니메이션 발생이 일어나지를 않아, 그리고 디자인 부분도 좀 고치면 좋을 것이, 로고가 너무 작아, 헤더의 로고 크기를 좀 더 키워주었으면 해(가로 길이 400px) 그리고 푸터 보면은 일단 푸터 바닥이 전체 웹사이트의 바닥에 위치하였으면 좋겠고, 달램이가 위치한 높이 값도 너무 높아 보여, 일단 푸터의 위에 바로 위치하는 형태면 좋아보여 그리고 푸터에 있는 카피라이크 글자도 가독성이 좋은 편이 아니야, 일단 푸터의 정 중앙에 위치하고 폰트 크기도 16px로 늘려, GEMINI.md 파일을 참고해보아도 좋아


7-2. 일단 디자인 부분 조금만 수정해보자, 일단 푸터 보면은 왼쪽만 가장자리가 부드러운데, 오른쪽도 일단 가장자리가 부드럽게 해봐, 그리고 헤더에 있는 양쪽의 로고 및 메뉴들 있잖아, 그 부분들의 레이아웃들이 가로 길이가 좀 긴 컴퓨터 화면에서 봤을 때엔느 맨 끝과 좀 차이가 별로 안나는데, 예를 들어서 왼쪽에 위치한 로고는 최대 1280px까지는 맨 왼쪽에 딱 붙고, 그 이후로는 떨어지다가 1440px부터는 80px 왼쪽으로 떨어지고 오른쪽에 위치한 메뉴들은 최대 1280px까지는 맨 오른쪽에 딱 붙고, 그 이후로는 떨어지다가 1440px부터는 80px 오른쪽으로 떨어지게 해


1. 이번에는 하루에 한번마다 로그인을 진행하면 